services:
  - type: web
    name: churn-api
    env: docker
    plan: free
    repo: https://github.com/andresglt/mlops-churn
    branch: main
    rootDir: .
    dockerfilePath: serving/Dockerfile.dockerfile
    autoDeploy: true
